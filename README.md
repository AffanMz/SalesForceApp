
  # Sales Force Application UI

  This is a code bundle for Sales Force Application UI. The original project is available at https://www.figma.com/design/1a5b7zEnGNgM9qj8WJzkno/Sales-Force-Application-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  